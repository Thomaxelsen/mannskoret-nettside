# AGENTS.md

> **Canonical source:** All project instructions are maintained in `CLAUDE.md` in this same directory. Read that file in full before starting any work. The instructions there apply to all AI agents regardless of provider.

This file exists so that tools like OpenAI Codex, which read `AGENTS.md` automatically, receive the same project context as Claude Code (which reads `CLAUDE.md`).
